# MisParse

## Setup

1. **Install dependencies**
   ```bash
   npm install
   ```

2. **Run the server**
   ```bash
   npm start
   ```
   Or: `node index.js`. The app listens on port 3000 by default.

3. **Access the challenge**
   Open `http://localhost:3000` in your browser.